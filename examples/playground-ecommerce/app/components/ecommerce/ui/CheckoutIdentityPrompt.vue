<script setup lang="ts">
/**
 * CheckoutIdentityPrompt — M5.2-T3 stub. M5.2-T4 replaces this with the real
 * PrimeVue-backed implementation. The stub ships a single emit so the
 * registry's event-name assertions hold and the import-graph test
 * has a file to scan (EJG-LAYOUT-1).
 */
import { defineComponent } from 'vue';
defineOptions({ name: 'CheckoutIdentityPrompt' });
void defineComponent;
</script>

<template>
  <div class="ecommerce-stub ecommerce-stub--CheckoutIdentityPrompt" data-stub="CheckoutIdentityPrompt">
    <span>CheckoutIdentityPrompt — pending M5.2-T4 implementation</span>
  </div>
</template>
